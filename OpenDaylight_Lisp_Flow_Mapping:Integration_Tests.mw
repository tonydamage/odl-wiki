This project includes these integration tests:
=== MapRegister === 
Send map register, and make sure we got the correct map notify.
=== MapRegister and MapREquest === 
Send map register, and then map request to the eid we registered and make sure we got the right result.

[[Category:LISP Flow Mapping]]
