{| align="right" border="1"
|align="center"|'''Related Pages'''
|-
|
* [[OpenDaylight Virtual Tenant Network (VTN):VTN Coordinator:RestApi|Introduction]]<br>
* [[OpenDaylight_Virtual_Tenant_Network_(VTN):VTN_Coordinator:RestApi#VTNCoordinator_RestApi_Contents|VTN Coordinator RestApi Contents]]<br>
* [[OpenDaylight Virtual Tenant Network (VTN):VTN Coordinator:RestApi:DHCP Relay Status Functions|DHCP Relay Status functions]]<br>
* [[OpenDaylight Virtual Tenant Network (VTN):VTN Coordinator:RestApi:DHCP Relay Interface Functions|DHCP Relay Interface functions]]<br>
* [[OpenDaylight Virtual Tenant Network (VTN):VTN Coordinator:RestApi:vRouter functions|vRouter functions]]<br>
|}


{| align="center" border="1" color="blue"
|align="center"|'''NOTE'''
|-
|This REST API is not supported in current release, its targeted for future release.
|}


=Create DHCP Relay Server=
This operation is used to create a DHCP Relay Server.

==Request==
{|class = "wikitable"
|-
|Method || POST
|-
|Request URI || 
* XML format
 /vtns/{vtn_name}/vrouters/{vrt_name}/dhcprelay/servers.xml
* JSON format
 /vtns/{vtn_name}/vrouters/{vrt_name}/dhcprelay/servers.json
|-
|Request body ||
* XML format
 <server ipaddr="{ipaddr}"/>
* JSON format
 {
  "server": {
   "ipaddr": "{ipaddr}"
   }
 }
|}

;Description of request URI elements
{|class = "wikitable"
|-
|Element || Description || Required
|-
|vtn_name || VTN name.
Valid value: Up to 31 characters that can include
alphabets, numbers, and underscore (except at the
beginning).
||Yes
|-
|vrt_name || vRouter name.
Valid value: Up to 31 characters that can include
alphabets, numbers, and underscore (except at the
beginning).
||Yes
|}

;Description of request URI elements
{|class = "wikitable"
|-
|Element || Description || Required
|-
|ipaddr ||IPv4 address.
Valid value: IPv4 dot-separated format (Example:
192.168.1.1)
||Yes
|}


;Response Body
:None

=Delete DHCP Relay Server=
This operation is used to delete a DHCP Relay Server.

==Request==
{|class = "wikitable"
|-
|Method || DELETE
|-
|Request URI || 
* XML format
 /vtns/{vtn_name}/vrouters/{vrt_name}/dhcprelay/servers/{ipaddr}.xml
* JSON format
 /vtns/{vtn_name}/vrouters/{vrt_name}/dhcprelay/servers/{ipaddr}.json
|}


;Description of request URI elements
{|class = "wikitable"
|-
|Element || Description || Required
|-
|vtn_name || VTN name.
Valid value: Up to 31 characters that can include
alphabets, numbers, and underscore (except at the
beginning).
||Yes
|-
|vrt_name || vRouter name.
Valid value: Up to 31 characters that can include
alphabets, numbers, and underscore (except at the
beginning).
||Yes
|-
|ipaddr || IPv4 address.
Valid value: IPv4 dot-separated format (Example:
192.168.1.1)
||Yes
|}

;Request Body
:None

==Response==
;Response Body
:None

=List DHCP Relay Servers=
This operation is used to list DHCP Relay Server information.

==Request==
{|class = "wikitable"
|-
|Method || GET
|-
|Request URI || 
* XML format
 /vtns/{vtn_name}/vrouters/{vrt_name}/dhcprelay/servers.xml
 /vtns/{vtn_name}/vrouters/{vrt_name}/dhcprelay/servers/count.xml
* JSON format
 /vtns/{vtn_name}/vrouters/{vrt_name}/dhcprelay/servers.json
 /vtns/{vtn_name}/vrouters/{vrt_name}/dhcprelay/servers/count.json
|-
|Query string ||
 ?index={ipaddr}&max_repetition={max_repetition}
|}


;Description of request URI elements
{|class = "wikitable"
|-
|Element || Description || Required
|-
|vtn_name || VTN name.
Valid value: Up to 31 characters that can include
alphabets, numbers, and underscore (except at the
beginning).
||Yes
|-
|vrt_name || vRouter name.
Valid value: Up to 31 characters that can include
alphabets, numbers, and underscore (except at the
beginning).
||Yes
|}

;Description of query string elements
{|class = "wikitable"
|-
|Element || Description || Required
|-
|ipaddr || IPv4 address.
Valid value: IPv4 dot-separated format (Example:
192.168.1.1)
||Yes
|-
|max_repetition ||The number of the resources that are returned.
Valid value: A positive integer.
Valid range: 1 to MAX of UINT32. Default is 10000.
||No
|}

;Request Body
:None

==Response==
{|class = "wikitable"
|-
|Response body ||
* XML format
If count is not specified in URI
 <?xml version="1.0" encoding="UTF-8" standalone="yes"?>
 <servers>
   <server ipaddr="{ipaddr}" />
 </servers>
If count is specified in URI
 <?xml version="1.0" encoding="UTF-8" standalone="yes"?>
 <servers count="{count}" />
• JSON format
If count is not specified in URI
 {
   "servers": [
     {
     "ipaddr": "{ipaddr}"
     }
   ]
 }
If count is specified in URI
 {
   "servers": {
       "count": "{count}"
   }
 }
|}

;Description of response elements
{|class = "wikitable"
|-
|Element || Description
|-
|server || Server
|-
|ipaddr || IPv4 address.
Valid value: IPv4 dot-separated format (Example: 192.168.1.1)
|-
|count || Number of Interfaces.
Valid value: A positive integer.
|}

=Show DHCP Relay Server=
This operation is used to view a specific DHCP Relay Server information

==Request==
{|class = "wikitable"
|-
|Method || GET
|-
|Request URI || 
* XML format
 /vtns/{vtn_name}/vrouters/{vrt_name}/dhcprelay/servers/{ipaddr}.xml
* JSON format
 /vtns/{vtn_name}/vrouters/{vrt_name}/dhcprelay/servers/{ipaddr}.json
|}


;Description of request URI elements
{|class = "wikitable"
|-
|Element || Description || Required
|-
|vtn_name || VTN name.
Valid value: Up to 31 characters that can include
alphabets, numbers, and underscore (except at the
beginning).
||Yes
|-
|vrt_name || vRouter name.
Valid value: Up to 31 characters that can include
alphabets, numbers, and underscore (except at the
beginning).
||Yes
|-
|ipaddr || IPv4 address.
Valid value: IPv4 dot-separated format (Example:
192.168.1.1)
||Yes
|}

;Request Body
:None

==Response==
;Response Body
{|class = "wikitable"
|-
|Response body ||
* XML format
 <?xml version="1.0" encoding="UTF-8" standalone="yes"?>
 <server ipaddr="{ipaddr}" />
* JSON format
 {
  "server": {
      "ipaddr": "{ipaddr}"
   }
 }
|}

;Description of response elements
{|class = "wikitable"
|-
|Element || Description
|-
|ipaddr || IPv4 address.
Valid value: IPv4 dot-separated format (Example: 192.168.1.1)
|}
[[Category:OpenDaylight Virtual Tenant Network]]
