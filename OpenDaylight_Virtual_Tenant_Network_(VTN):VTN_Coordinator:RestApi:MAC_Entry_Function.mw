=Show MAC Entries=
This operation is used to view a specific MAC Entry information.

==Request==
{|class = "wikitable"
|-
|Method || GET
|-
|Request URI || /vtns/{vtn_name}/vbridges/{vbr_name}/macentries
/vtns/{vtn_name}/vbridges/{vbr_name}/macentries/
count
|-
|Query String || ? type={type}
|}

===Description of request URI elements===
{|class = "wikitable"
|-
|Element || Description ||Required
|-
|vtn_name ||VTN name.
Valid value: Up to 31
characters that can include
alphabets, numbers, and
underscore (except at the
beginning).
||Yes
|-
|vbr_name ||vBridge name.
Valid value: Up to 31
characters that can include
alphabets, numbers, and
underscore (except at the
beginning).
||Yes
|}

===Description of query string elements===
{|class = "wikitable"
|-
|Element || Description ||Required
|-
|type || The type of MAC address.
Valid value: static,
dynamic
|| No
|}
===Request body===
:None

==Response==
==Response==
{|class = "wikitable"
|-
|Response Body(XML format) ||
If count is not specified in URI
 <?xml version="1.0"
  encoding="UTF-8"
  standalone="yes"?>
 <macentries>
 [<macentry macaddr="{macaddr}"
  type="{type}"
  if_name="{if_name}"
  if_kind="{if_kind}"/>]
 </macentries>

If count is specified in URI
 <?xml version="1.0"
  encoding="UTF-8"
  standalone="yes"?>
 <macentries count="{count}"/>
|-
|Response Body(JSON format) ||
If count is not specified in URI
 {
  "macentries": [
     {
          "macaddr":
     "{macaddr}",
          "type": "{type}",
          "if_name":
     "{if_name}",
          "if_kind": "{if_kind}"
     }
   ]
 }

If count is specified in URI
 {
 "macentries": {
      "count": "{count}"
     }
 }
|}
