{| align="right" border="1"
|align="center"|'''Related Pages'''
|-
|
* [[OpenDaylight Virtual Tenant Network (VTN):VTN Coordinator:RestApi|Introduction]]<br>
* [[OpenDaylight_Virtual_Tenant_Network_(VTN):VTN_Coordinator:RestApi#VTNCoordinator_RestApi_Contents|VTN Coordinator RestApi Contents]]<br>
* [[OpenDaylight Virtual Tenant Network (VTN):VTN Coordinator:RestApi: VTN Functions|VTN functions]]<br>
* [[OpenDaylight Virtual Tenant Network (VTN):VTN Coordinator:RestApi:vBypass Interface Functions|vBypass Interface functions]]<br>
|}


{| align="center" border="1" color="blue"
|align="center"|'''NOTE'''
|-
|This REST API is not supported in current release, its targeted for future release.
|}


=Create vBypass=
This operation is used to create a vBypass.

==Request==
{|class = "wikitable"
|-
|Method || POST
|-
|Request URI || 
* XML format
 /vtns/{vtn_name}/vbypasses.xml
* JSON format
 /vtns/{vtn_name}/vbypasses.json
|-
|Request body ||
* XML format
 <vbypass vbypass_name="{vbypass_name}" domain_id="{domain_id}
  description="{description}" type="{type}""/>
* JSON format
 {
  "vbypass": {
          "vbypass_name": "{vbypass_name}",
          "domain_id": "{domain_id}"
          "description": "{description}",
          "type": "{type}",
    }
  }
|}

;Description of request URI element
{|class ="wikitable"
|-
|Element || Description || Required
|-
|vtn_name ||VTN name.
Valid value: Up to 31 characters that can include
alphabets, numbers, and underscore (except at the
beginning).
||Yes
|}

;Description of request elements
{|class = "wikitable"
|-
|Element || Description || Required
|-
|vbypass_name || vBypass name.
Valid value: Up to 31 characters that can include
alphabets, numbers, and underscore (except at the
beginning).
||Yes
|-
|description ||Additional information.
Valid value: A string of up to 127 characters.
||No
|-
|type ||Type of vBypass node.
Valid value: bridge, router.
||No
|-
|domain_id ||Domain identifier.
Valid value: Up to 31 characters that can include
alphabets, numbers, and underscore (except at the
beginning).
Note: The value of domain_id is case sensitive.
||Yes
|}

*Restriction
(DEFAULT) cannot be specified in domain_id. If (DEFAULT) is specified, the behavior is undefined.

==Response==
;Response Body
:None

=Delete vBypass=
This operation is used to delete a vBypass.

==Request==
{|class = "wikitable"
|-
|Method || DELETE
|-
|Request URI || 
* XML format
 /vtns/{vtn_name}/vbypasses/{vbypass_name}.xml
* JSON format
 /vtns/{vtn_name}/vbypasses/{vbypass_name}.json
|}

;Description of request URI elements
{|class = "wikitable"
|-
|Element || Description || Required
|-
|vtn_name || VTN name.
Valid value: Up to 31 characters that can include
alphabets, numbers, and underscore (except at the
beginning).
||Yes
|-
|vbypass_name || vBypass name.
Valid value: Up to 31 characters that can include
alphabets, numbers, and underscore (except at the
beginning).
||Yes
|}

;Request Body
:None

==Response==
;Response Body
:None

=Update vBypass=
This operation is used to update a vBypass.

==Request==
{|class = "wikitable"
|-
|Method || PUT
|-
|Request URI || 
* XML format
 /vtns/{vtn_name}/vbypasses/{vbypass_name}.xml
* JSON format
 /vtns/{vtn_name}/vbypasses/{vbypass_name}.json
|-
|Request body ||
* XML format
 <vbypass description="{description}" type="{type}" />
* JSON format
 {
    "vbypass": {
          "description": "{description}",
          "type": "{type}",
       }
  }
|}


;Description of request URI elements
{|class = "wikitable"
|-
|Element || Description || Required
|-
|vtn_name || VTN name.
Valid value: Up to 31 characters that can include
alphabets, numbers, and underscore (except at the
beginning).
||Yes
|-
|vbypass_name || vBypass name.
Valid value: Up to 31 characters that can include
alphabets, numbers, and underscore (except at the
beginning).
||Yes
|}

;Description of request elements
{|class = "wikitable"
|-
|Element || Description || Required
|-
|description || Additional information.
Valid value: A string of up to 127 characters.
||No
|-
|type || Type of vBypass node.
Valid value: bridge, router
||No
|}

*Restriction
(DEFAULT) cannot be specified in domain_id. If (DEFAULT) is specified, the behavior is undefined.

==Response==
;Response Body
:None

=List vBypass=
This operation is used to list vBypass information based on specified conditions.

==Request==
{|class = "wikitable"
|-
|Method || GET
|-
|Request URI || 
* XML format
 /vtns/{vtn_name}/vbypasses.xml
 /vtns/{vtn_name}/vbypasses/detail.xml
 /vtns/{vtn_name}/vbypasses/count.xml
* JSON format
 /vtns/{vtn_name}/vbypasses.json
 /vtns/{vtn_name}/vbypasses/detail.json
 /vtns/{vtn_name}/vbypasses/count.json
|-
|Query string ||
 ?index={vbypass_name}&max_repetition={max_repetition}
|}

;Description of request URI elements
{|class = "wikitable"
|-
|Element || Description || Required
|-
|vtn_name || VTN name.
Valid value: Up to 31 characters that can include
alphabets, numbers, and underscore (except at the
beginning).
||Yes
|}

;Description of query string elements
{|class = "wikitable"
|-
|Element || Description || Required
|-
|vbypass_name || vBypass name.
Valid value: Up to 31 characters that can include
alphabets, numbers, and underscore (except at the
beginning).
||No
|-
|max_repetition || Number of the resources that are returned.
Valid value: A positive integer
Valid range: 1 to MAX of UINT32. Default is 10000.
||No
|}

;Request Body
:None

==Response==
{|class = "wikitable"
|-
|Response body || 
* XML format
If countor details is not specified in URI
 <?xml version="1.0" encoding="UTF-8" standalone="yes"?>
 <vbypasses>
    <vbypass vbypass_name="{vbypass_name}"/>
  </vbypasses>

If detail is specified in URI
 <?xml version="1.0" encoding="UTF-8" standalone="yes"?>
 <vbypasses>
   <vbypass vbypass_name="{vbypass_name}" domain_id="{domain_id}" description="{description}" t
    ype="{type}" />
 </vbypasses>

If count is specified in URI
 <?xml version="1.0" encoding="UTF-8" standalone="yes"?>
 <vbypasses count="{count}"/>

• JSON format
If count is not specified in URI
 {
  "vbypasses": [
      {
      "vbypass_name": "{vbypass_name}"
      }
   ]
 }

If detail is specified in URI
 {
   "vbypasses": [
     {
      "vbypass_name": "{vbypass_name}",
      "domain_id": "{domain_id}",
      "description": "{description}",
      "type": "{type}",
      }
    ]
 }

If count is specified in URI
 { 
   "vbypasses": {
        "count": "{count}"
   }
 }
|}

;Description of response elements
{|class = "wikitable"
|-
|Element || Description
|-
|vbypass_name || vBypass name.
Valid value: Up to 31 characters that can include alphabets,
numbers, and underscore (except at the beginning).
description Additional information.
Valid value: A string of up to 127 characters.
|-
|type ||Type of vBypass node.
Valid value: bridge, router.
|-
|domain_id ||Domain identifier.
Valid value: Up to 31 characters that can include alphabets,
numbers, and underscore (except at the beginning).
Note: The value of domain_id is case sensitive.
|-
|count || The number of vBypass.
Valid value: A positive integer.
|}

=Show vBypass=
This operation is used to view a specific vBypass information.

==Request==
{|class = "wikitable"
|-
|Method || GET
|-
|Request URI || 
* XML format
 /vtns/{vtn_name}/vbypasses/{vbypass_name}.xml
* JSON format
 /vtns/{vtn_name}/vbypasses/{vbypass_name}.json
|}


;Description of request URI element
{|class ="wikitable"
|-
|Element || Description || Required
|-
|vtn_name ||VTN name.
Valid value: Up to 31 characters that can include
alphabets, numbers, and underscore (except at the
beginning).
||Yes
|-
|vbypass_name || vBypass name.
Valid value: Up to 31 characters that can include
alphabets, numbers, and underscore (except at the
beginning).
||Yes
|}

;Request Body
:None

==Response==
{|class = "wikitable"
|-
|Response body ||
* XML format
 <?xml version="1.0" encoding="UTF-8" standalone="yes"?>
 <vbypass vbypass_name="{vbypass_name}" domain_id="{domain_id}" description="{description}" t
  ype="{type}" />

• JSON format
 {
   "vbypass": {
      "vbypass_name": "{vbypass_name}",
      "domain_id": "{domain_id}"
      "description": "{description}",
      "type": "{type}",
    }
 }
|}


;Description of response elements
{|class = "wikitable"
|-
|Element || Description
|-
|vbypass_name || vBypass name.
Valid value: Up to 31 characters that can include alphabets,
numbers, and underscore (except at the beginning).
description Additional information.
Valid value: A string of up to 127 characters.
|-
|type ||Type of vBypass node.
Valid value: bridge, router.
|-
|domain_id ||Domain identifier.
Valid value: Up to 31 characters that can include alphabets,
numbers, and underscore (except at the beginning).
Note: The value of domain_id is case sensitive.
|}
[[Category:OpenDaylight Virtual Tenant Network]]
