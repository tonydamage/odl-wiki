=List Mappings=
This operation is used to list VTN mappings information

==Request==
{|class = "wikitable"
|-
|Request URI ||
/vtns/{vtn_name}/mappings
/vtns/{vtn_name}/mappings/detail
|-
|Query String || ?index={mapping_id}
&max_repetition={max_repetition}
|}

===Description of request URI element===
{|class = "wikitable"
|-
|vtn_name || VTN name.
Valid value: Up to 31
characters that can include
alphabets, numbers, and
underscore (except at the
beginning).
||Yes
|}

===Description of query string elements===
{|class = "wikitable"
|-
|mapping_id || Mapping identifier.
Valid value: A string
with the format
{controller_id}-
{domain_id}.
||Yes
|-
|max_repetition || Number of the resources
that are returned.
Valid value: A positive
integer
||No
|}

===Request body===
:None


==Resoponse==
{|class = "wikitable"
|-
|Response body (XML format)||
If detail is not specified in URI
 <?xml version="1.0"
  encoding="UTF-8"
  standalone="yes"?>
 <mappings>
   <mapping mapping_id
 ="{mapping_id}"/>
 </mappings>

If detail is specified in URI
 <?xml version="1.0"
  encoding="UTF-8"
  standalone="yes"?>
 <mappings>
 <mapping
  mapping_id="{mapping_id}"
  controller_id="{controller_id}"
  domain_id="{domain_id}">
 <mappinginfos>
 <mappinginfo
  switch_id="{switch_id}"
  port_name="{port_name}"
  logical_port_id="{logical_port_id}"
  vlan_id="{vlan_id}"
  tagged="{tagged}"
  type="{type}"
  vbr_name="{vbr_name}"
  if_name="{if_name}"/>
 </mappinginfos>
 </mapping>
 </mappings>

|-
|Response body(JSON format) ||
If detail is not specified in URI
 {
  "mappings": [
    {
       "mapping_id":
    "{mapping_id}"
    }
   ]
 }

If detail is specified in URI
 {
  "mappings": [
      {
       "mapping_id":
    "{mapping_id}",
       "controller_id":
    "{controller_id}",
       "domain_id":
    "{domain_id}",
       "mappinginfos": [
            {
              "switch_id":
          "{switch_id}",
              "port_name":
          "{port_name}",
              "logical_port_id":
          "{logical_port_id}",
              "vlan_id":
          "{vlan_id}",
              "tagged":
          "{tagged}",
              "type":
          "{type}",
              "vbr_name":
          "{vbr_name}",
              "if_name":
          "{if_name}"
           }
        ]
      }
    ]
  }
|}

===Description of response elements(mappings)===

{|class = "wikitable"
|-
|Element || Description
|-
|mapping_id || Mapping identifier.
Valid value: A string with
the format {controller_id}-
{domain_id}.
|-
|controller_id || Identifier of the Controller.
Valid values: Up to 31
characters that can include
alphabets, numbers, and underscore (except at the
beginning).
|-
|domain_id || Domain identifier.
Valid value: Up to 31
characters that can include
alphabets, numbers, and
underscore (except at the
beginning) and the string -
(DEFAULT).
Note: The value of domain_id
is case sensitive.
|-
|mappinginfos || Mapping data corresponding
to specific Controller and
Domain.
|}

===Description of response elements(mappinginfos)===
{|class = "wikitable"
|-
|switch_id || switch_id Identifier of the Switch.
Valid value: A string of up to
255 characters.
|-
|port_name || Port name.
Valid value: A string of up to
31 characters.
|-
|logical_port_id || Identifier of the Logical Port.
Valid value: A string of up to
319 characters.
|-
|vlan_id || VLAN identifier.
Valid value: A positive integer.
Valid range: 1 - 4095
|-
|tagged || Displays whether VLAN tags
are sent and received in the
Physical network.
Valid value:
• true: Send and receive
packets with VLAN tags.
• false: Send and receive
packets without VLAN tags.
|-
|type || Type of mapping.
Valid value: static, dynamic
|-
|vbr_name || vBridge name.
Valid value: Up to 31
characters that can include
alphabets, numbers, and
underscore (except at the
beginning).
|-
|if_name ||Interface name.
Valid value: Up to 31
characters that can include
alphabets, numbers, and
underscore (except at the
beginning).
|}

=Show Mapping=
This operation is used to view the mapping information.


==Request==
{|class = "wikitable"
|-
|Request URI ||
/vtns/{vtn_name}/mappings/{mapping_id}
|}

===Description of request URI element===
{|class = "wikitable"
|-
|vtn_name || VTN name.
Valid value: Up to 31
characters that can include
alphabets, numbers, and
underscore (except at the
beginning).
||Yes
|-
|mapping_id || Mapping identifier.
Valid value: A string
with the format
{controller_id}-
{domain_id}.
||Yes
|}

===Request body===
:None

==Response==

{|class = "wikitable"
|-
|Response body (XML format) ||
 <?xml version="1.0"
  encoding="UTF-8"
  standalone="yes"?>
 <mapping mapping_id="{mapping_id}"
  controller_id="{controller_id}"
  domain_id="{domain_id}">
 <mappinginfos>
      [
 <mappinginfo
  switch_id="{switch_id}"
  port_name="{port_name}"
  logical_port_id="{logical_port_id}"
  vlan_id="{vlan_id}"
  tagged="{tagged}"
  type="{type}"
  vbr_name="{vbr_name}"
  if_name="{if_name}"/>
      ]
 </mappinginfos>
 <mapping/>
|-
|Response body (JSON format) ||
 {
   "mapping": {
       "mapping_id":
   "{mapping_id}",
       "controller_id":
   "{controller_id}",
       "domain_id":
   "{domain_id}",
       "mappinginfos": [
            {
                "switch_id":
             "{switch_id}",
                "port_name":
             "{port_name}",
                "logical_port_id":
             "{logical_port_id}",
                "vlan_id":
             "{vlan_id}",
                "tagged":
             "{tagged}",
                "type": "{type}",
                "vbr_name":
                "{vbr_name}",
                "if_name":
                "{if_name}"
            }
          ]
       }
  }
|}

===Description of response elements(mappings)===

{|class = "wikitable"
|-
|Element || Description
|-
|mapping_id || Mapping identifier.
Valid value: A string with
the format {controller_id}-
{domain_id}.
|-
|controller_id || Identifier of the Controller.
Valid values: Up to 31
characters that can include
alphabets, numbers, and underscore (except at the
beginning).
|-
|domain_id || Domain identifier.
Valid value: Up to 31
characters that can include
alphabets, numbers, and
underscore (except at the
beginning) and the string -
(DEFAULT).
Note: The value of domain_id
is case sensitive.
|-
|mappinginfos || Mapping data corresponding
to specific Controller and
Domain.
|}

===Description of response elements(mappinginfos)===
{|class = "wikitable"
|-
|switch_id || switch_id Identifier of the Switch.
Valid value: A string of up to
255 characters.
|-
|port_name || Port name.
Valid value: A string of up to
31 characters.
|-
|logical_port_id || Identifier of the Logical Port.
Valid value: A string of up to
319 characters.
|-
|vlan_id || VLAN identifier.
Valid value: A positive integer.
Valid range: 1 - 4095
|-
|tagged || Displays whether VLAN tags
are sent and received in the
Physical network.
Valid value:
• true: Send and receive
packets with VLAN tags.
• false: Send and receive
packets without VLAN tags.
|-
|type || Type of mapping.
Valid value: static, dynamic
|-
|vbr_name || vBridge name.
Valid value: Up to 31
characters that can include
alphabets, numbers, and
underscore (except at the
beginning).
|-
|if_name ||Interface name.
Valid value: Up to 31
characters that can include
alphabets, numbers, and
underscore (except at the
beginning).
|}
