{| align="right" border="1"
|align="center"|'''Related Pages'''
|-
|
* [[OpenDaylight Virtual Tenant Network (VTN):VTN Coordinator:RestApi|Introduction]]<br>
* [[OpenDaylight_Virtual_Tenant_Network_(VTN):VTN_Coordinator:RestApi#VTNCoordinator_RestApi_Contents|VTN Coordinator RestApi Contents]]<br>
* [[OpenDaylight Virtual Tenant Network (VTN):VTN Coordinator:RestApi: VTN Functions|VTN functions]]<br>
* [[OpenDaylight Virtual Tenant Network (VTN):VTNCoordinator:RestApi:VTN Flow Filter Entry functions|VTN Flow Filter Entry functions]]<br>
|}


{| align="center" border="1" color="blue"
|align="center"|'''NOTE'''
|-
|This REST API is not supported in current release, its targetted for future release.
|}


=List VTN Mappings=
This operation is used to list VTN mappings information.

==Request==
{|class = "wikitable"
|-
|Method || GET
|-
|Request URI || 
 /vtns/{vtn_name}/mappings
 /vtns/{vtn_name}/mappings/detail
|-
|Query String ||
 ?index={mapping_id}
 &max_repetition={max_repetition}
|}

;Description of request URI elements
{|class = "wikitable"
|-
|Element || Description || Required
|-
|vtn_name || VTN name.
Valid value: Up to 31 characters that can include
alphabets, numbers, and underscore (except at the
beginning).
||No
|}

;Description of Query String Elements.
{|class = "wikitable"
|-
|Element || Description ||Required
|-
|mapping_id ||Mapping ID.
Valid value: A string with the format {controller_id}-
{domain_id}.
||No
|-
|max_repetition || Number of the resources that will be returned. The
default value is 10000.
Valid value: Decimal number.
Valid range: 1 - UINT32_MAX
||No
|}

==Response==
;Response Body
{|class = "wikitable"
|-
|Response Body ||
*XML format
If detail is not specified in URI
 <?xml version="1.0" encoding="UTF-8" standalone="yes"?>
 <mappings>
    <mapping mapping_id ="{mapping_id}"/>
 </mappings>
If detail is specified in URI
 <?xml version="1.0" encoding="UTF-8" standalone="yes"?>
 <mappings>
 <mapping mapping_id="{mapping_id}" 
   controller_id="{controller_id}"
   domain_id="{domain_id}">
 <mappinginfos>
 <mappinginfo switch_id="{switch_id}"
   port_name="{port_name}"
   logical_port_id="{logical_port_id}"
   vlan_id="{vlan_id}"
   tagged="{tagged}"
   maptype="{maptype}"
   vbr_name="{vbr_name}"
   if_name="{if_name}"/>
  </mappinginfos>
 </mapping>
 <mappings>

* JSON format
If detail is not specified in URI
 {
 "mappings": [
 {
  "mapping_id": "{mapping_id}"
  }
 ]
 }
If detail is specified in URI
 {
 "mappings": [
  {
  "mapping_id": "{mapping_id}",
  "controller_id": "{controller_id}",
  "domain_id": "{domain_id}",
  "mappinginfos": [
     {
    "switch_id": "{switch_id}",
    "port_name": "{port_name}",
    "logical_port_id": "{logical_port_id}",
    "vlan_id": "{vlan_id}",
    "tagged": "{tagged}",
    "maptype": "{maptype}",
    "vbr_name": "{vbr_name}",
    "if_name": "{if_name}"
     }
    ]
   }
  ]
 }
|}
;Description of response elements (mappings)
{|class = "wikitable"
|Element || Description
|-
|mapping_id || Mapping ID.
Valid value: A string with the format {controller_id}-
{domain_id}.
|-
|controller_id || Controller ID.
Valid value: Up to 31 characters including alphanumeric
characters and underscore (except at the beginning).
|-
|domain_id || Domain ID.
Valid value: Up to 31 characters including alphanumeric
characters and underscore (except at the beginning), and the
string "(DEFAULT)".
Note: "(DEFAULT)" indicates default domain.
|-
|mappinginfos ||Mapping data corresponding to specific Controller and Domain.
|}

;Description of response elements (mappinginfos)
{|class = "wikitable"
|-
|Element || Description
|-
|switch_id ||Switch ID.
Valid value: Up to 255 characters.
|-
|port_name || Port name.
Valid value: Up to 31 characters.
|-
|logical_port_id || Logical Port ID.
Valid value: Up to 319 characters.
|-
|vlan_id || VLAN ID.
Valid value: Decimal number.
Valid range: 1 - 4095
|-
|tagged ||Tagged flag.
Valid value:
• true: Send and receive packets with VLAN tag.
• false: Send and receive packets without VLAN tag.
|-
|maptype || Map type.
Valid value: portmap, vlanmap
|-
|vbr_name || vBridge name.
Valid value: Up to 31
characters including alphanumeric
characters and underscore (except at the beginning).
|-
|if_name || Interface name.
Valid value: Up to 31 characters including alphanumeric
characters and underscore (except at the beginning).
|}

=Show VTN Mapping=
This operation is used to view the mapping information

==Request==
{|class = "wikitable"
|-
|Method || GET
|-
|Request URI || 
/vtns/{vtn_name}/mappings/{mapping_id}
|}

;Description of request URI elements
{|class = "wikitable"
|-
|Element || Description || Required
|-
|vtn_name || VTN name.
Valid value: Up to 31 characters including
alphanumeric characters and underscore (except at the
beginning).
||Yes
|-
|mapping_id ||Mapping ID.
Valid value: A string with the format {controller_id}-
{domain_id}.
||Yes
|}

==Response==
;Response Body
{|class = "wikitable"
|-
|Response Body ||
* XML format
 <?xml version="1.0" encoding="UTF-8" standalone="yes"?>
 <mapping mapping_id="{mapping_id}"
  controller_id="{controller_id}"
  domain_id="{domain_id}">
 <mappinginfos>
 <mappinginfo switch_id="{switch_id}"
   port_name="{port_name}"
   logical_port_id="{logical_port_id}"
   vlan_id="{vlan_id}"
   tagged="{tagged}"
   maptype="{maptype}"
   vbr_name="{vbr_name}"
   if_name="{if_name}"/>
  </mappinginfos>
 <mapping/>
* JSON format
 {
 "mapping": {
   "mapping_id": "{mapping_id}",
   "controller_id": "{controller_id}",
   "domain_id": "{domain_id}",
   "mappinginfos": [
       {
   "switch_id":
     "switch_id": "{switch_id}",
     "port_name": "{port_name}",
     "logical_port_id": "{logical_port_id}",
     "vlan_id": "{vlan_id}",
     "tagged": "{tagged}",
     "maptype": "{maptype}",
     "vbr_name": "{vbr_name}",
     "if_name": "{if_name}"
     }
   ]
  }
 }


;Description of response elements (mappings)
{|class = "wikitable"
|Element || Description
|-
|mapping_id || Mapping ID.
Valid value: A string with the format {controller_id}-
{domain_id}.
|-
|controller_id || Controller ID.
Valid value: Up to 31 characters including alphanumeric
characters and underscore (except at the beginning).
|-
|domain_id || Domain ID.
Valid value: Up to 31 characters including alphanumeric
characters and underscore (except at the beginning), and the
string "(DEFAULT)".
Note: "(DEFAULT)" indicates default domain.
|-
|mappinginfos ||Mapping data corresponding to specific Controller and Domain.
|}

;Description of response elements (mappinginfos)
{|class = "wikitable"
|-
|Element || Description
|-
|switch_id ||Switch ID.
Valid value: Up to 255 characters.
|-
|port_name || Port name.
Valid value: Up to 31 characters.
|-
|logical_port_id || Logical Port ID.
Valid value: Up to 319 characters.
|-
|vlan_id || VLAN ID.
Valid value: Decimal number.
Valid range: 1 - 4095
|-
|tagged ||Tagged flag.
Valid value:
• true: Send and receive packets with VLAN tag.
• false: Send and receive packets without VLAN tag.
|-
|maptype || Map type.
Valid value: portmap, vlanmap
|-
|vbr_name || vBridge name.
Valid value: Up to 31
characters including alphanumeric
characters and underscore (except at the beginning).
|-
|if_name || Interface name.
Valid value: Up to 31 characters including alphanumeric
characters and underscore (except at the beginning).
|}

[[Category:OpenDaylight Virtual Tenant Network]]
