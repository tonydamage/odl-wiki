{| align="right" border="1"
|align="center"|'''Related Pages'''
|-
|
* [[OpenDaylight Virtual Tenant Network (VTN):VTN Coordinator:RestApi|Introduction]]<br>
* [[OpenDaylight_Virtual_Tenant_Network_(VTN):VTN_Coordinator:RestApi#VTNCoordinator_RestApi_Contents|VTN Coordinator RestApi Contents]]<br>
* [[OpenDaylight Virtual Tenant Network (VTN):VTN Coordinator:RestApi:L2 Network Example Using VTN Virtualization|L2 Network Example Using VTN Virtualization]]
|}

= About =
This Page will list the REST API to create/update/delete vTerminal Interface(s) to [[OpenDaylight Virtual Tenant Network (VTN):VTN Coordinator|VTN Coordinator]]
 
= List of Functions Supported =
== Create vTerminal Inetface ==
This operation is used to create a vTerminal Interface
=== Request ===
 {| class="wikitable"
|-
| Method || POST
|-
| Request URI   || 
 /vtns/{vtn_name}/vterminals/{vterminal_name}/interfaces
|-
| Request Body (XML)  ||
  <interface if_name="{if_name}" description="{description}" adminstat
 us="{adminstatus}"/>
|-
| Request Body (JSON) ||
 {
  "interface": {
     "if_name": "{if_name}",
     "description": "{description}",
     "adminstatus": "{adminstatus}"
   }
 }
|}

;Description of Request URI Elements
{| class="wikitable"
|-
| Element || Description || Required
|-
|vtn_name || VTN name.
Valid value: Up to 31 characters including
alphanumeric characters and underscore (except at the
beginning).
||Yes
|-
|vterminal_name || vTerminal name.
Valid value: Up to 31 characters including
alphanumeric characters and underscore (except at the
beginning).
||Yes
|}

; Description of Request Elements 
{| class="wikitable"
|-
| Element || Description || Required
|-
|if_name || vTerminal Interface name.
Valid value: Up to 31 characters including
alphanumeric characters and underscore (except at the
beginning).
||Yes
|-
|description || Additional information.
Valid value: Up to 127 characters.
||No
|-
|adminstatus || Admin status.
Valid value: enable, disable
||No
|}

=== Response ===

: The Response returned will be one of the values in the [[OpenDaylight Virtual Tenant Network (VTN):VTN Coordinator:RestApi|List of response Codes]].
 
; Response Body 
:None

* Remember
Only one vTerminal Interface can be created for one vTerminal.

* Important
PFC does not support adimin status. Do not specify adiminstatus when the type of Controller is "pfc". If
the adminstatus is set by mistake, delete the vTerminal Interface and create a new vTerminal Interface.

==Update vTerminal Interface==
This operation is used to update a vTerminal Interface.

===Request===

{| class="wikitable"
|-
| Method || PUT
|-
| Request URI   || 
 /vtns/{vtn_name}/vterminals/{vterminal_name}/interfaces/{if_name}
|-
| Request Body (XML)  ||
  <interface description="{description}" adminstatus="{adminstatus}"/>
|-
| Request Body (JSON) ||
 {
   "interface": {
   "description": "{description}",
   "adminstatus": "{adminstatus}"
   }
 }
|}

;Description of Request Elements
{|class = "wikitable"
|-
|description || A small description text for the VTN.
Valid value: A string of up to 127 characters. 
||No
|-
|adminstatus || Admin status.
Valid value: enable, disable
||No
|}

=== Response ===

: The Response returned will be one of the values in the [[OpenDaylight Virtual Tenant Network (VTN):VTN Coordinator:RestApi|List of response Codes]].
 
;Response Body 
:None

*Important
PFC does not support adimin status. Do not specify adiminstatus when the type of Controller is "pfc". If
the adminstatus is set by mistake, delete the vTerminal Interface and create a new vTerminal Interface.

==List vTerminal Interface ==
This operation is used to list vTerminal Interface information based on specified conditions.


===Request===

{| class="wikitable"
|-
| Method || GET
|-
| Request URI   || 
/vtns/{vtn_name}/vterminals/{vterminal_name}/interfaces
/vtns/{vtn_name}/vterminals/{vterminal_name}/interfaces/detail
/vtns/{vtn_name}/vterminals/{vterminal_name}/interfaces/count
|- 
| Query string ||
 ?index={if_name}
 &max_repetition={max_repetition}
|}

;Description of request URI elements

{|class="wikitable"
|-
| Element || Description || Required
|-
|vtn_name || VTN name.
Valid value: Up to 31 characters including
alphanumeric characters and underscore (except at the
beginning).
||Yes
|-
|vterminal_name || vTerminal name.
Valid value: Up to 31 characters including
alphanumeric characters and underscore (except at the
beginning).
||Yes
|}
;Description of query string elements

{| class="wikitable"
|-
| Element || Description || Required
|-
|if_name || vTerminal Interface name.
Valid value: Up to 31 characters including
alphanumeric characters and underscore (except at the
beginning).
||No
|-
|max_repetetion || Number of the resources that are returned.Valid value: A positive integer Valid range: 1 to MAX of UINT32. Default is 10000
||No
|}

=== Response ===

: The Response returned will be one of the values in the [[OpenDaylight Virtual Tenant Network (VTN):VTN Coordinator:RestApi|List of response Codes]].
 
;Response Body 
{| class="wikitable"
|-
| Response Body (XML)  ||
If count or detail is not specified in URI
  <?xml version="1.0" encoding="UTF-8" standalone="yes"?>
  <interfaces>
     <interface if_name="{if_name}"/>
  </interfaces>
If detail is specified in URI
 <?xml version="1.0" encoding="UTF-8" standalone="yes"?>
 <interfaces>
    <interface if_name="{if_name}" description="{description}" admins
 tatus="{adminstatus}" operstatus="{operstatus}"/>
 </interfaces>
If count is specified in URI
 <?xml version="1.0" encoding="UTF-8" standalone="yes"?>
 <interfaces count="{count}"/>
|-
| Request Body (JSON) ||
If count or detail is not specified in URI
 {
 "interfaces": [
   {
    "if_name": "{if_name}"
   }
  ]
 }
If detail is specified in URI
 {
 "interfaces": [
    {
     "if_name": "{if_name}",
     "description": "{description}",
     "adminstatus": "{adminstatus}",
     "operstatus": "{operstatus}"
    }
  ]
 }
If count is specified in URI
 {
   "interfaces": {
      "count": "{count}"
   }
 }
|}

;Description of Response Elements

{| class="wikitable"
|-
| Element || Description 
|-
|if_name || vTerminal Interface name.
Valid value: Up to 31 characters including alphanumeric
characters and underscore (except at the beginning).
|-
|description || Additional information.
Valid value: Up to 127 characters.
|-
|adminstatus || Admin status.
Valid value: enable, disable
|-
|operstatus || Operational status.
Valid value: up, down, unknown
|-
|count || Number of the vTerminal Interfaces.
Valid value: Decimal number.
|}

== Show vTerminal Interface(s) ==
This operation is used to view a specific vTerminal Interface information.

=== Request ===
 
{| class="wikitable"
|-
| Method || GET
|-
| Request URI   || 
 /vtns/{vtn_name}/vterminals/{vterminal_name}/interfaces/{if_name}
|}

;Description of Request Elements


{| class="wikitable"
|-
| Element || Description || Required
|-
|vtn_name || VTN name Valid value: Up to 31 characters that can include alphabets, numbers, and underscore (except at the beginning). 
||Yes
|-
|vterminal_name || vTerminal name.
Valid value: Up to 31 characters including
alphanumeric characters and underscore (except at the
beginning).
||Yes
|-
|if_name || vTerminal Interface name.
Valid value: Up to 31 characters including
alphanumeric characters and underscore (except at the
beginning).
||Yes
|}

=== Response ===

: The Response returned will be one of the values in the [[OpenDaylight Virtual Tenant Network (VTN):VTN Coordinator:RestApi|List of response Codes]].
 
;Response Body
{| class="wikitable"
|-
| Response Body (XML)  ||
  <?xml version="1.0" encoding="UTF-8" standalone="yes"?>
 <interface if_name="{if_name}" description="{description}" adminsta
 tus="{adminstatus}" operstatus="{operstatus}"/>
|-
| Request Body (JSON) ||
  {
  "interface": {
     "if_name": "{if_name}",
     "description": "{description}",
     "adminstatus": "{adminstatus}",
     "operstatus": "{operstatus}"
   }
  }
|}

;Description of Response Elements 
{| class="wikitable"
|-
| Element || Description 
|-
|if_name ||vTerminal Interface name.
Valid value: Up to 31 characters including alphanumeric
characters and underscore (except at the beginning).
|-
|description || Additional information.
Valid value: Up to 127 characters.
|-
|adminstatus || Admin status.
Valid value: enable, disable
|-
|operstatus || Operational status.
Valid value: up, down, unknown
|}

==Delete vTerminal Interface==
This operation is used to delete a vTerminal Interface.

===Request===

{| class="wikitable"
|-
| Method || DELETE
|-
| Request URI   || 
 /vtns/{vtn_name}/vterminals/{vterminal_name}/interfaces/{if_name}
|}

; Description of Request Elements 
{| class="wikitable"
|-
| Element || Description || Required
|-
|vtn_name || VTN name Valid value: Up to 31 characters that can include alphabets, numbers, and underscore (except at the beginning). 
||Yes
|-
|vterminal_name || vTerminal name.
Valid value: Up to 31 characters including
alphanumeric characters and underscore (except at the
beginning).
||Yes
|-
|if_name || vTerminal Interface name.
Valid value: Up to 31 characters including
alphanumeric characters and underscore (except at the
beginning).
|| Yes
|}

=== Response ===

: The Response returned will be one of the values in the [[OpenDaylight Virtual Tenant Network (VTN):VTN Coordinator:RestApi|List of response Codes]].
 
;Response Body 
:None
[[Category:OpenDaylight Virtual Tenant Network]]
